# 云电脑价格扫描系统与日报系统整合方案

**版本**：v1.0  
**日期**：2026-04-26  
**目标**：零重复建设、复用现有架构、数据统一流转

> 2026-04-28 运行修正：本文中的三条原始业务线继续保留，不删除。
> - 市场追踪日报：06:00 生成数据和图片，10:00 正式推送。
> - 白天异动检测：07:30 / 12:00 / 17:30 执行。
> - 晚间全量检测：21:00 执行 A+B 层22个机型深度爬取。
> - 云电脑扫描：每2小时执行，独立输出 `cloud_pc_daily.json` 后并入日报。
> 当前统一调度真源为 `config/schedule.json`，兼容入口见 `白天异动检测/`、`scripts/run_daytime_scan.py`、`scripts/run_cloud_pc_scan.py`。

---

## 一、现有系统分析

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           日程调度层                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 市场追踪日报  │  │ 白天异动检测 │  │ 云电脑扫描   │  │ 其他系统...  │  │
│  │  每天06:00   │  │ 7:30/12/17:30│  │  每2小时     │  │              │  │
│  │              │  │   /21:00    │  │              │  │              │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────────────┘  │
└─────────┼─────────────────┼─────────────────┼────────────────────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           数据存储层                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐            │
│  │  日报目录      │  │  异动检测目录   │  │  云电脑监控目录 │            │
│  │ ./YYYY_MM_DD/  │  │ ./白天异动检测/ │  │ ./cloud-pc-    │            │
│  │                │  │                 │  │    monitor/   │            │
│  └────────────────┘  └────────────────┘  └────────────────┘            │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                    【新建】统一数据缓存层                            │ │
│  │                    ./data/price_cache.json                          │ │
│  │                    ./data/baseline.json                             │ │
│  └────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           输出展示层                                      │
│                    推送至飞书/邮件/Coze消息                             │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 现有模块职责

| 模块 | 日程ID | 职责 | 输出 |
|------|--------|------|------|
| 市场追踪日报 | - | 资讯整合、信号分级、收购建议 | `./2026_04_26/*.md` |
| 白天异动检测 | - | A/B层价格异动检测、mobile_use验证 | `./白天异动检测/日报_*.md` |
| 云电脑扫描 | 7632883696076603690 | 轻量价格扫描、异常检测 | `./cloud-pc-monitor/logs/*.log` |

### 1.3 现有配置分析

#### 品类配置（`品类配置.json`）
```json
{
  "categories": {
    "运动相机": { "items": [...], "change_threshold": 10.0 },
    "显卡": { "items": [...], "change_threshold": 10.0 },
    ...
  },
  "分层策略": { "A层_高频": [...], "B层_低频": [...] }
}
```

#### 云电脑阈值配置（`thresholds.json`）
```json
{
  "confidence": { "trigger_cloud_phone": 70, "weights": {...} },
  "anomaly": { "algorithms": {"three_sigma": 0.25, ...}, "vote_threshold": 3 }
}
```

#### 基准线数据（`baseline.json`）
```json
{
  "baselines": {
    "RTX_3070": { "min": 3899, "max": 4999, "avg": 4299 },
    ...
  }
}
```

---

## 二、整合架构设计

### 2.1 核心原则

1. **不新建独立日报**：云电脑扫描数据作为章节整合进现有日报
2. **统一数据源**：所有价格数据写入统一缓存文件
3. **复用检测逻辑**：异动算法在两个系统间共享
4. **渐进式迁移**：保留云电脑现有功能，逐步迁移

### 2.2 目录结构重组

```
./运动相机追踪/
├── config/                          # 【新建】统一配置目录
│   ├── categories.json              # 合并后的品类配置
│   ├── thresholds.json              # 合并后的阈值配置
│   └── baseline.json                # 价格基准线
├── data/                            # 【新建】统一数据目录
│   ├── price_cache.json             # 实时价格缓存
│   ├── anomaly_votes.json           # 异动投票记录
│   └── trend_history/               # 历史趋势数据
│       └── YYYY_MM_DD.json
├── 2026_04_26/                      # 日报目录（已存在）
│   └── 20260426_0604_市场追踪日报.md
├── 白天异动检测/                     # 异动检测（已存在）
│   ├── 品类配置.json                # 【迁移至config/】
│   └── 日报_*.md
└── cloud-pc-monitor/                # 云电脑扫描（已存在）
    ├── config/                      # 【保留，仅作参考】
    ├── data/                        # 【保留，仅作参考】
    └── logs/                        # 【保留，仅作参考】
```

### 2.3 数据流设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              数据流                                      │
│                                                                          │
│  ┌──────────────┐     读取      ┌──────────────────┐     写入          │
│  │ 云电脑扫描    │ ──────────▶ │  统一数据缓存      │ ◀────────────────┤
│  │ (每2小时)     │             │  price_cache.json │     白天异动检测   │
│  └──────────────┘             └──────────────────┘     (mobile_use验证) │
│          │                              ▲                       │       │
│          │                              │                       │       │
│          │         读取                 │          读取         │       │
│          ▼                              │                       ▼       │
│  ┌──────────────┐             ┌──────────────────┐     ┌────────────────┐│
│  │  异动检测     │ ◀────────── │  统一配置层       │ ──▶ │  日报生成      ││
│  │  (5种算法)    │             │  categories.json │     │  (每天06:00)   ││
│  └──────────────┘             │  thresholds.json │     └────────────────┘│
│                               └──────────────────┘              │       │
└───────────────────────────────────────────────────────────────────────│───┘
                                                                        │
                                                                        ▼
                                                          ┌────────────────────┐
                                                          │    飞书/日报推送    │
                                                          └────────────────────┘
```

---

## 三、配置统一方案

### 3.1 合并后的品类配置（`config/categories.json`）

```json
{
  "version": "3.0.0",
  "updated_at": "2026-04-26",
  "sources": {
    "运动相机": { "from": "白天异动检测", "migrated": true },
    "显卡": { "from": "白天异动检测", "migrated": true },
    "CPU": { "from": "白天异动检测", "migrated": true },
    "内存固态": { "from": "白天异动检测", "migrated": true },
    "云电脑专项": { "from": "cloud-pc-monitor", "migrated": true }
  },
  "categories": {
    "运动相机": {
      "items": [
        {"id": "dji_pocket3", "name": "DJI Pocket 3", "stop_threshold": 1400, "change_threshold": 10.0},
        {"id": "dji_pocket4", "name": "DJI Pocket 4", "stop_threshold": 2200, "change_threshold": 10.0}
      ],
      "detection_priority": "B"
    },
    "显卡": {
      "items": [
        {"id": "rtx_3060", "name": "RTX 3060", "stop_threshold": 900, "change_threshold": 10.0},
        {"id": "rtx_3070", "name": "RTX 3070", "stop_threshold": 1400, "change_threshold": 10.0}
      ],
      "detection_priority": "A"
    },
    "云电脑专项": {
      "items": [
        {"id": "gpu_3090", "name": "RTX 3090 整机", "stop_threshold": null, "change_threshold": 15.0},
        {"id": "gpu_4090", "name": "RTX 4090 整机", "stop_threshold": null, "change_threshold": 15.0},
        {"id": "workstation", "name": "工作站云电脑", "stop_threshold": null, "change_threshold": 20.0}
      ],
      "detection_priority": "C",
      "note": "云电脑扫描重点关注，低频检测"
    }
  },
  "detection_schedule": {
    "A层_高频": {"times": ["07:30", "12:00", "17:30"], "source": "白天异动检测"},
    "B层_中频": {"times": ["21:00"], "source": "晚间全量检测"},
    "C层_低频": {"times": ["08:00", "10:00", "12:00", "14:00", "16:00", "18:00", "20:00", "22:00"], "source": "云电脑扫描"}
  }
}
```

### 3.2 合并后的阈值配置（`config/thresholds.json`）

```json
{
  "version": "2.0.0",
  "updated_at": "2026-04-26",
  "merged_from": ["白天异动检测/品类配置.json", "cloud-pc-monitor/config/thresholds.json"],
  "confidence": {
    "trigger_cloud_phone": 70,
    "direct_record": 90,
    "weights": {
      "data_source": 0.25,
      "sample_size": 0.20,
      "consistency": 0.20,
      "trend": 0.20,
      "cross_validation": 0.15
    }
  },
  "anomaly": {
    "algorithms": {
      "three_sigma": { "weight": 0.25, "enabled": true },
      "trend_break": { "weight": 0.25, "enabled": true },
      "distribution_shift": { "weight": 0.20, "enabled": true },
      "isolation_forest": { "weight": 0.15, "enabled": true },
      "iqr": { "weight": 0.15, "enabled": true }
    },
    "vote_threshold": 3,
    "adaptive_threshold": {
      "enabled": true,
      "min_samples": 7,
      "learning_rate": 0.1
    }
  },
  "batch": {
    "min_models_for_batch": 3,
    "min_hours_since_last": 2
  },
  "alert_levels": {
    "INFO": { "vote_count": 1, "action": "记录" },
    "WARN": { "vote_count": 2, "action": "记录+日报" },
    "ALERT": { "vote_count": 3, "action": "立即推送" },
    "CRITICAL": { "vote_count": 4, "action": "立即推送+云手机验证" }
  }
}
```

### 3.3 统一价格缓存（`data/price_cache.json`）

```json
{
  "updated_at": "2026-04-26T14:00:00+08:00",
  "sources": {
    "cloud_pc_scan": { "last_run": "2026-04-26T14:00:00+08:00", "model_count": 22 },
    "daytime_anomaly": { "last_run": "2026-04-26T12:00:00+08:00", "model_count": 8 }
  },
  "prices": {
    "RTX_3070": {
      "current": { "min": 1480, "max": 1899, "avg": 1650, "source": "nickys", "updated": "2026-04-26" },
      "baseline": { "min": 3899, "max": 4999, "avg": 4299, "source": "ZOL", "updated": "2026-04-26" },
      "change_pct": -38.3,
      "anomaly_votes": 4,
      "stop_threshold": 1400
    }
  },
  "alerts": []
}
```

---

## 四、分析能力增强

### 4.1 异动算法融合（5种算法投票）

现有系统已有：
- `three_sigma`：标准差检测
- `trend_break`：趋势突破检测
- `distribution_shift`：分布偏移检测
- `isolation_forest`：孤立森林（需训练）
- `iqr`：四分位距检测

**整合方案**：
1. 将云电脑的算法封装为独立函数
2. 合并到统一的 `anomaly_detector.py`
3. 投票结果写入 `anomaly_votes.json`

```python
# 伪代码：异动检测核心逻辑
def detect_anomaly(model_id, price_data, baseline, config):
    votes = []
    
    if config["three_sigma"]["enabled"]:
        votes.append(three_sigma_check(price_data, baseline))
    
    if config["trend_break"]["enabled"]:
        votes.append(trend_break_check(price_data))
    
    # ... 其他算法
    
    vote_count = sum(v["anomaly"] * v["weight"] for v in votes)
    threshold = config["vote_threshold"]
    
    return {
        "model_id": model_id,
        "anomaly": vote_count >= threshold,
        "vote_count": vote_count,
        "details": votes
    }
```

### 4.2 自适应阈值调整

**原理**：基于历史数据自动调整检测阈值

```json
{
  "adaptive_threshold": {
    "enabled": true,
    "rules": [
      {
        "condition": "样本数 >= 14天",
        "action": "切换到动态阈值（均值±2σ）"
      },
      {
        "condition": "波动率 > 30%",
        "action": "临时放宽阈值50%"
      },
      {
        "condition": "连续3天异动",
        "action": "触发云手机验证"
      }
    ]
  }
}
```

### 4.3 趋势分析能力

**历史数据积累**：
```json
{
  "trend_history": {
    "YYYY_MM_DD": {
      "RTX_3070": { "avg": 1650, "change_pct": -2.1, "trend": "下跌" },
      "DDR5_16G": { "avg": 280, "change_pct": +1.2, "trend": "上涨" }
    }
  }
}
```

**日报展示**：
```
### 📈 趋势分析

| 品类 | 近7日趋势 | 近30日趋势 | 预测 |
|------|----------|-----------|------|
| RTX 3070 | ↓ -8.3% | ↓↓ -38.3% | 继续下跌 |
| DDR5 16G | ↑ +5.1% | ↑↑ +20% | 震荡上行 |
```

---

## 五、日报整合方案

### 5.1 日报新增章节

在现有日报末尾添加：

```markdown
---

## 五、云电脑价格扫描（14:00更新）

### 扫描概况
- **执行时间**：2026-04-26 14:00
- **监控范围**：22个机型（C层云电脑专项：3个）
- **数据来源**：search_web轻量扫描

### 价格数据

| 品类 | 当前价 | 基准价 | 变动 | 状态 |
|------|--------|--------|------|------|
| RTX 3090 整机 | ¥12,999 | ¥15,999 | -18.8% | ⚠️ 待确认 |
| RTX 4090 整机 | ¥19,999 | ¥22,999 | -13.0% | ✅ 正常 |
| 工作站云电脑 | ¥8.9/时 | ¥9.5/时 | -6.3% | ✅ 正常 |

### 异动分析
- **异动检测**：RTX 3090整机价格下跌18.8%
- **投票结果**：3/5算法确认（触发阈值）
- **建议动作**：触发云手机深度验证

### 置信度评估
- 数据源：ZOL + 多电商平台（置信度 82%）
- 样本量：多商家报价（置信度 75%）
- **综合置信度**：78%

---

## 执行日志
| 项目 | 内容 |
|-----|------|
| 云电脑扫描 | ✅ 完成（14:00，22个机型） |
| 异动检测 | ⚠️ 1个待确认（RTX 3090） |
| 待办 | RTX 3090云手机验证（已排队） |
```

### 5.2 日报生成流程修改

```
现有流程：
资讯追踪日程 → 采集资讯 → 生成日报

修改后流程：
资讯追踪日程 → 采集资讯 → 读取价格缓存 → 生成日报（含云电脑章节）
```

**关键修改点**：
1. 日报生成脚本增加 `读取价格缓存` 步骤
2. 在日报末尾添加云电脑章节
3. 缓存文件包含当日所有扫描结果

---

## 六、需要修改的文件清单

### 6.1 新建文件

| 文件路径 | 说明 |
|----------|------|
| `./运动相机追踪/config/categories.json` | 合并后的品类配置 |
| `./运动相机追踪/config/thresholds.json` | 合并后的阈值配置 |
| `./运动相机追踪/config/baseline.json` | 价格基准线 |
| `./运动相机追踪/data/price_cache.json` | 统一价格缓存 |
| `./运动相机追踪/data/anomaly_votes.json` | 异动投票记录 |
| `./运动相机追踪/data/trend_history/` | 历史趋势数据目录 |
| `./运动相机追踪/整合方案文档.md` | 本文档 |

### 6.2 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `./运动相机追踪/白天异动检测/品类配置.json` | 标记为 deprecated，迁移配置到 config/ |
| `./运动相机追踪/cloud-pc-monitor/config/thresholds.json` | 标记为 deprecated |
| `./运动相机追踪/cloud-pc-monitor/scripts/` | 改造为写入统一缓存 |
| 日报生成脚本 | 增加读取缓存、输出云电脑章节 |

### 6.3 保留文件（仅供参考）

| 文件路径 | 说明 |
|----------|------|
| `./运动相机追踪/白天异动检测/品类配置.json` | 保留参考，不修改 |
| `./运动相机追踪/cloud-pc-monitor/config/` | 保留参考，不修改 |
| `./运动相机追踪/cloud-pc-monitor/logs/` | 保留历史日志 |

---

## 七、具体实施步骤

### 阶段一：配置统一（Day 1）

1. **创建统一配置目录**
   ```bash
   mkdir -p ./运动相机追踪/config
   mkdir -p ./运动相机追踪/data/trend_history
   ```

2. **合并品类配置**
   - 读取 `白天异动检测/品类配置.json`
   - 读取 `cloud-pc-monitor/config/models.json`
   - 生成 `config/categories.json`

3. **合并阈值配置**
   - 读取 `cloud-pc-monitor/config/thresholds.json`
   - 合并算法配置和告警策略
   - 生成 `config/thresholds.json`

4. **迁移基准线**
   - 读取 `cloud-pc-monitor/data/baseline.json`
   - 复制到 `config/baseline.json`

### 阶段二：数据层改造（Day 2）

1. **创建统一价格缓存**
   - 初始化 `data/price_cache.json`
   - 结构：包含各数据源的最后更新时间

2. **改造云电脑扫描脚本**
   - 修改输出：从日志改为写入 `price_cache.json`
   - 保留日志文件作为备份

3. **改造白天异动检测**
   - 读取 `price_cache.json`
   - 统一写入价格数据

### 阶段三：日报整合（Day 3）

1. **修改日报生成逻辑**
   - 日报生成前读取 `price_cache.json`
   - 在日报末尾添加云电脑章节

2. **测试数据流转**
   - 云电脑扫描 → 缓存 → 日报
   - 验证数据完整性

### 阶段四：算法增强（Day 4-5）

1. **实现自适应阈值**
   - 收集14天历史数据
   - 实现动态阈值切换

2. **实现趋势分析**
   - 每日保存趋势快照
   - 日报展示近7日/30日趋势

---

## 八、约束条件检查

| 约束 | 状态 | 说明 |
|------|------|------|
| 零外部依赖 | ✅ | 仅使用现有API |
| 不增加额外成本 | ✅ | 复用现有日程和配额 |
| 复用现有推送渠道 | ✅ | 使用现有飞书推送 |
| 避免过度设计 | ✅ | 最小化改动，渐进迁移 |

---

## 九、风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 配置迁移出错 | 日报生成失败 | 保留原配置，设置回滚机制 |
| 缓存文件损坏 | 数据丢失 | 每日备份，历史日志保底 |
| 阈值调整误报 | 推送过多 | 初期使用保守阈值 |

---

## 十、后续优化方向

1. **机器学习模型**：基于历史数据训练价格预测模型
2. **多源交叉验证**：增加数据源提升置信度
3. **智能推送**：根据置信度自动选择推送渠道
4. **可视化面板**：生成价格趋势图表

---

*文档版本：v1.0 | 创建时间：2026-04-26*
